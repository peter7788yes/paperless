<UserControl x:Class="PaperLess_Emeeting.MenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="50" Margin="10,5,0,10">
    <Grid>
        <Image x:Name="btnImg" Panel.ZIndex="1" Width="50" Height="60" Stretch="Uniform" SnapsToDevicePixels="True"    />
        <TextBlock x:Name="txtBtnName" Panel.ZIndex="2" FontSize="12" Width="50"  Foreground="White"  TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center"   Margin="0,43,0,0" >
            <TextBlock.Effect>
                <DropShadowEffect
                                  ShadowDepth="1"
                                  Direction="315"
                                  Color="Black"
                                  Opacity="0.75"
                                  BlurRadius="1.0" />
            </TextBlock.Effect>
        </TextBlock>
    </Grid>
</UserControl>
